[package]
name = "xore-lang"
version = "0.1.0"
edition = "2021"
description = "Xore language compiler — lexer + parser + codegen"

[[bin]]
name = "xore"
path = "src/main.rs"

[lib]
name = "xore_lang"
path = "src/lib.rs"

[dev-dependencies]
pretty_assertions = "1.4"